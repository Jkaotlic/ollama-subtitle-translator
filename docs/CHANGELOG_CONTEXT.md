# 🎯 Обновление: Добавлена поддержка контекста перевода

## Что нового?

Добавлена возможность задавать **контекст** перед переводом субтитров. Теперь LLM модель учитывает специфику контента при переводе.

## Изменённые файлы

### 1. `templates/index.html`
- ➕ Добавлено текстовое поле для ввода контекста
- 📝 Плейсхолдер с примером: "Это сериал The Pitt о больнице, много медицинской терминологии"
- 📤 Контекст передаётся в форме при отправке

### 2. `app.py`
- ➕ Функция `translate_worker` теперь принимает параметр `context`
- 🔄 Промпт для LLM модифицирован для учёта контекста
- 📥 Эндпоинт `/translate` получает контекст из формы

### 3. `translate_srt.py`
- ➕ Класс `Translator` теперь принимает параметр `context`
- 🔄 Метод `translate` использует контекст в промпте
- 📝 Добавлен аргумент командной строки `--context` / `-c`

### 4. `CONTEXT_EXAMPLES.md` (новый файл)
- 📖 Примеры использования контекста
- 💡 Рекомендации по написанию контекста
- 🎬 Примеры для разных жанров (медицина, IT, юриспруденция и т.д.)

## Как использовать

### Веб-интерфейс
1. Загрузите .srt файл
2. В поле "Контекст" введите описание, например:
   - *"Это сериал The Pitt о больнице, много медицинской терминологии"*
   - *"Юридический сериал о корпоративных юристах"*
   - *"Научно-фантастический сериал, действие в космосе"*
3. Выберите язык перевода
4. Нажмите "Перевести"

### Командная строка
```bash
# Базовое использование с контекстом
python translate_srt.py movie.srt -c "Медицинский сериал о врачах"

# Полный пример
python translate_srt.py episode.srt \
  --lang Russian \
  --context "Это сериал The Pitt. Медицинская драма с больничной терминологией"
```

## Формат промпта с контекстом

**Без контекста:**
```
Translate the following segment into Russian, without additional explanation.

[текст субтитров]
```

**С контекстом:**
```
Context: Это сериал The Pitt. Медицинская драма с больничной терминологией

Translate the following segment into Russian, keeping the context in mind. Provide only the translation without additional explanation.

[текст субтитров]
```

## Преимущества

✅ **Улучшенная точность** для специализированной терминологии  
✅ **Сохранение контекста** между блоками субтитров  
✅ **Учёт жанра** и специфики контента  
✅ **Опциональность** - можно не заполнять, если не нужно  

## Обратная совместимость

✅ Все изменения обратно совместимы  
✅ Если контекст не указан, работает как раньше  
✅ Существующие скрипты и команды продолжат работать  

## Примеры контекстов

- Медицина: *"Медицинский сериал, много диагнозов и процедур"*
- IT: *"Сериал о хакерах и кибербезопасности"*
- Юриспруденция: *"Юридическая драма о судебных процессах"*
- История: *"Исторический сериал о королевской семье"*
- Sci-Fi: *"Космическая опера, технический жаргон"*

Подробнее см. [CONTEXT_EXAMPLES.md](CONTEXT_EXAMPLES.md)
