# 🎬 Переводчик субтитров

Локальный переводчик субтитров на базе **HuanYuan MT 1.5** (Tencent) — победитель WMT25.  
Работает **без API ключей**, полностью офлайн после загрузки модели.

## ⚡ Быстрый старт

### На русский (по умолчанию):
```bash
./translate.sh "movie.srt"
```

### На другой язык:
```bash
./translate.sh "movie.srt" Japanese
./translate.sh "movie.srt" German
./translate.sh "movie.srt" French
```

---

## 🛠 Полная команда

```bash
python translate_srt_hf.py "файл.srt" [опции]
```

### Опции:
| Флаг | Описание | По умолчанию |
|------|----------|--------------|
| `-l`, `--lang` | Целевой язык | `Russian` |
| `-o`, `--out` | Выходной файл | `<input>.<lang>.srt` |
| `-m`, `--model` | Модель перевода | `tencent/HY-MT1.5-1.8B` |
| `-b`, `--batch` | Размер батча | `5` |

### Примеры:
```bash
# Перевод на русский
python translate_srt_hf.py movie.srt

# Перевод на японский с указанием выходного файла
python translate_srt_hf.py movie.srt -l Japanese -o movie_jp.srt

# Использование более качественной модели (7B)
python translate_srt_hf.py movie.srt -m tencent/HY-MT1.5-7B
```

---

## 🌍 Поддерживаемые языки

| Язык | Код | | Язык | Код |
|------|-----|-|------|-----|
| Russian | ru | | Japanese | ja |
| English | en | | Korean | ko |
| Chinese | zh | | German | de |
| French | fr | | Spanish | es |
| Italian | it | | Portuguese | pt |
| Turkish | tr | | Arabic | ar |
| Thai | th | | Vietnamese | vi |
| Polish | pl | | Dutch | nl |
| Ukrainian | uk | | ... | ... |

Всего **33 языка** + 5 диалектов.

---

## 📦 Модели

| Модель | Размер | RAM | Качество |
|--------|--------|-----|----------|
| `tencent/HY-MT1.5-1.8B` | ~4 GB | ~8 GB | ⭐⭐⭐⭐ |
| `tencent/HY-MT1.5-1.8B-GPTQ-Int4` | ~1 GB | ~4 GB | ⭐⭐⭐ |
| `tencent/HY-MT1.5-7B` | ~14 GB | ~18 GB | ⭐⭐⭐⭐⭐ |
| `tencent/HY-MT1.5-7B-GPTQ-Int4` | ~4 GB | ~8 GB | ⭐⭐⭐⭐ |

---

## 🔧 Установка (один раз)

```bash
# Создать виртуальное окружение
python3 -m venv .venv
source .venv/bin/activate

# Установить зависимости
pip install transformers==4.56.0 torch accelerate

# Сделать скрипт исполняемым
chmod +x translate.sh
```

---

## 💡 Советы

- **Первый запуск** занимает ~5-7 минут (загрузка модели ~4 GB)
- Последующие запуски быстрее — модель кэшируется
- На Mac с M1/M2/M3 используется GPU (MPS) автоматически
- Для лучшего качества используйте модель 7B: `-m tencent/HY-MT1.5-7B`
